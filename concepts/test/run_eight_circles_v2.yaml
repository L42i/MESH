---
- name: Copy and run Processing sketch
  hosts: all
  become: true

  vars:
    user_name: "student"
    sketch_name: "eight_circles"

    # remote sketch paths
    sketch_dir: "/home/{{ user_name }}/Desktop/MESH/concepts/test{{ sketch_name }}"
    
  tasks:

    - name: Run Processing sketch as {{ user_name }}
      become: true
      become_user: "{{ user_name }}"
      ansible.builtin.command: processing-java --sketch={{ sketch_dir }} --run